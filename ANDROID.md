# تطبيق أندرويد (APK) — دليل سريع

التطبيق مغلّف بـ [Capacitor](https://capacitorjs.com/) بحيث يتحوّل موقع الويب إلى
تطبيق أندرويد مستقل (APK) يُثبَّت على هواتف المناديب.

- **معرّف التطبيق:** `com.hawafel.salestracker`
- **الاسم الظاهر:** متابعة المبيعات
- المشروع الأصلي (Android) موجود في مجلد `android/`.

---

## الطريقة الأسهل: بناء الـ APK على GitHub (بدون تثبيت أي شيء)

### مرة واحدة فقط: أضف سرّين للمستودع
من: `Settings → Secrets and variables → Actions → New repository secret`
أضف (نفس القيم الموجودة في ملف `.env`):

| الاسم | القيمة |
|---|---|
| `REACT_APP_SUPABASE_URL` | رابط مشروع Supabase |
| `REACT_APP_SUPABASE_KEY` | مفتاح anon العام |

### كل مرة تريد APK جديد
1. افتح تبويب **Actions** في المستودع.
2. اختر **Build Android APK** من القائمة.
3. اضغط **Run workflow**.
4. بعد ما ينتهي (بضع دقائق)، انزل لقسم **Artifacts** وحمّل **`sales-tracker-apk`** — بداخله ملف `app-debug.apk`.

---

## التوزيع على المناديب
- أرسل ملف `app-debug.apk` (واتساب/إيميل/رابط).
- على الهاتف: افتح الملف → اسمح بـ **«تثبيت من مصادر غير معروفة»** للتطبيق الذي يفتح الملف → ثبّت.
- عند فتح التطبيق أول مرة سيطلب أذونات **الموقع** و**الكاميرا** — اقبلها لتسجيل الزيارات وإرفاق الصور.

> الـ APK الحالي **debug** (موقّع بمفتاح تجريبي) — يعمل ويُثبَّت بشكل طبيعي للاستخدام الداخلي.
> لنشر رسمي على Google Play أو توقيع بمفتاح إنتاج، نضيف keystore ونبني `assembleRelease` (خطوة لاحقة).

---

## تحديث التطبيق لاحقًا
هذا الـ APK **يحتوي نسخة الويب وقت البناء**، فأي ميزة جديدة تحتاج بناء APK جديد
(كرر خطوات Actions أعلاه) وإعادة إرساله للمناديب.

> بديل (تحديث تلقائي دون إعادة توزيع): توجيه التطبيق لرابط الموقع المباشر عبر
> `server.url` في `capacitor.config.json`. عندها يكفي بناء الـ APK مرة واحدة وكل
> تحديث ويب يصل للمناديب فورًا. أخبرني برابط الموقع لتفعيلها.

---

## بناء محلي (اختياري، لمن لديه Android Studio)
```bash
npm install
npm run android:apk
# الناتج: android/app/build/outputs/apk/debug/app-debug.apk
```
